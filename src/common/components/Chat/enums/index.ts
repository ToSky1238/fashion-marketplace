export enum ChatCategory {
  BLOCKED = "blocked",
  ARCHIEVED = "archieved",
  ALL = "all",
  UNREAD = "unread",
}

export enum ChatStatus {
  ACTIVE = "active",
  BLOCKED = "blocked",
  ARCHIEVED = "archieved",
}

export enum MessageStatus {
  SEEN = "seen",
  UNSEEN = "unseen",
}

export enum UserStatus {
  ONLINE = "online",
  OFFLINE = "offline",
}
